# Compiled binaries (built by `go build`, and by the release pipeline later)
/cvetrace
/cvetrace.exe
/dist/

# Gradle's own local build cache, created when the fixture's wrapper actually
# runs (see test/fixtures/gradle-fixture-project) -- not something to commit.
.gradle/
